version: '3.8'

services:
  humblelegend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite:///./humblelegend.db
      - LOG_LEVEL=INFO
    restart: unless-stopped